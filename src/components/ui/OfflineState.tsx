/**
 * @file: src/components/ui/OfflineState.tsx
 * @description:
 *   Экран «нет интернета» — ОДИН на всё приложение.
 *
 *   ЗАЧЕМ ОТДЕЛЬНЫМ КОМПОНЕНТОМ. С 1.5.38 React Query знает о состоянии сети
 *   (см. `@/lib/query-bridges`) и без неё не бросает запрос, а ставит его на
 *   паузу: ошибки нет, данных нет. Экран в этот момент показывал своё обычное
 *   «пусто» — «Свободных заказов нет», «Поездок пока нет», «Способы вывода не
 *   настроены». Это не просто неинформативно, это НЕПРАВДА: мы про заказы,
 *   поездки и способы вывода сейчас ничего не знаем.
 *
 *   Восемь экранов писали бы это по-своему и разошлись бы формулировками —
 *   как уже было с тремя самодельными баннерами связи до 1.5.17. Здесь один
 *   текст и один значок.
 *
 *   КОГДА ПОКАЗЫВАТЬ: сети нет И данных ещё нет. Если данные есть, пусть и
 *   устаревшие, — показывать их, а не заглушку: водителю на дороге прошлый
 *   список полезнее пустого экрана. Обновятся сами, как только связь
 *   вернётся.
 *
 * @dependencies: ./EmptyState
 * @created: 2026-09-07 (1.5.38)
 */

import { EmptyState } from './EmptyState';

interface OfflineStateProps {
  /**
   * Что именно сейчас не показать — именительный падеж, с большой буквы:
   * «Заказы», «Поездки», «Данные профиля». Подставляется в пояснение, поэтому
   * должно читаться как подлежащее: «Заказы появятся здесь сами…».
   */
  what: string;
}

export function OfflineState({ what }: OfflineStateProps) {
  return (
    <EmptyState
      icon="cloud-offline-outline"
      tone="danger"
      title="Нет интернета"
      description={`${what} появятся здесь сами, как только связь вернётся`}
    />
  );
}
